package br.com.caelum.argentum;

import java.util.Calendar;

public class CandleBuilder {
/*
	private double abertura;
	private double fechamento;
	private double minimo;
	private double maximo;
	private double volume;
	private Calendar data;
	private int count;
	
	public CandleBuilder(){
		count = 0;
	}
	
	public CandleBuilder abertura(double abertura){
		if(abertura < 0) throw new IllegalArgumentException("Abertura deve ser maior que 0");
		this.abertura = abertura;
		count++;
		return this;
	}
	
	public CandleBuilder fechamento(double fechamento){
		if(fechamento < 0) throw new IllegalArgumentException("Fechamento deve ser maior que 0");
		this.fechamento = fechamento;
		count++;
		return this;
	}
	
	public CandleBuilder minimo(double minimo){
		if(minimo < 0) throw new IllegalArgumentException("Minimo deve ser maior que 0");
		this.minimo = minimo;
		count++;
		return this;
	}
	
	public CandleBuilder maximo(double maximo){
		if(maximo < 0) throw new IllegalArgumentException("Maximo deve ser maior que 0");
		this.maximo = maximo;
		count++;
		return this;
	}
	
	public CandleBuilder volume(double volume){
		if(volume < 0) throw new IllegalArgumentException("Volume deve ser maior que 0");
		this.volume = volume;
		count++;
		return this;
	}
	
	public CandleBuilder data(Calendar data){
		if(data == null) throw new IllegalArgumentException("Data nao deve ser nula");
		this.data = data;
		count++;
		return this;
	}
	
	public Candle geraCandle(){
		if(maximo < minimo) throw new IllegalArgumentException("Maximo deve ser maior que minimo");
		if(count != 6) throw new IllegalArgumentException("Todos os valores devem ser preenchidos");
		return new Candle(abertura, fechamento, minimo, maximo, volume, data);
	}
	*/
}
